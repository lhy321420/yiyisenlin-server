<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.FriendRequestMapper">

    <insert id="insertRequest">
        INSERT INTO friend_request (from_user_id, to_user_id, status)
        VALUES (#{fromUserId}, #{toUserId}, 0)
    </insert>

    <select id="getPendingRequests" resultType="com.example.demo.entity.FriendRequest">
        SELECT
            id,
            from_user_id AS fromUserId,
            to_user_id AS toUserId,
            status,
            create_time AS createTime
        FROM friend_request
        WHERE to_user_id = #{toUserId} AND status = 0
        ORDER BY create_time DESC
    </select>

    <select id="countPendingRequests" resultType="int">
        SELECT COUNT(*) FROM friend_request WHERE to_user_id = #{toUserId} AND status = 0
    </select>

    <update id="updateStatus">
        UPDATE friend_request SET status = #{status} WHERE id = #{id}
    </update>

    <select id="checkPending" resultType="int">
        SELECT COUNT(*) FROM friend_request
        WHERE from_user_id = #{fromUserId} AND to_user_id = #{toUserId} AND status = 0
    </select>

    <select id="selectById" resultType="com.example.demo.entity.FriendRequest">
        SELECT
            id,
            from_user_id AS fromUserId,
            to_user_id AS toUserId,
            status,
            create_time AS createTime
        FROM friend_request
        WHERE id = #{id}
    </select>

</mapper>