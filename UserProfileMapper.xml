<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.UserProfileMapper">
    <select id="getByUserId" resultType="com.example.demo.entity.UserProfile">
        SELECT * FROM user_profile WHERE user_id = #{userId}
    </select>
    <insert id="insertOrUpdate">
        INSERT INTO user_profile (user_id, status, photos, recent_activity)
        VALUES (#{userId}, #{status}, #{photos}, #{recentActivity})
            ON DUPLICATE KEY UPDATE
                                 status = VALUES(status), photos = VALUES(photos), recent_activity = VALUES(recent_activity)
    </insert>
</mapper>